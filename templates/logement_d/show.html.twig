{% extends 'base.html.twig' %}

{% block title %}LogementD{% endblock %}

{% block body %}
    <h1>LogementD</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ logement_d.id }}</td>
            </tr>
            <tr>
                <th>NbPlace</th>
                <td>{{ logement_d.nbPlace }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ logement_d.emplacement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_logement_d_index') }}">back to list</a>

    <a href="{{ path('app_logement_d_edit', {'id': logement_d.id}) }}">edit</a>

    {{ include('logement_d/_delete_form.html.twig') }}
{% endblock %}
