<form method="post" action="{{ path('app_logement_d_delete', {'id': logement_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logement_d.id) }}">
    <button class="btn">Delete</button>
</form>
